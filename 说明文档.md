# Fermi GBM + LAT 联合光谱分析 — 项目说明

本文档说明重构后的工程布局、配置方式与运行入口。物理流程与原版 `gbmtest_enhanced1.py` 保持一致；实现代码集中在 **`grb_project`** 包中。

---

## 1. 目录与入口

| 路径 | 说明 |
|------|------|
| `gbmtest_enhanced1_refactored.py` | **兼容入口**：命令行行为与旧版一致，并导出 `DATA_DIR`、`RESULT_ROOT`、`main`、`analyze_grb` 等模块级名称。 |
| `grb_project/` | **核心实现包**（见下表）。 |
| `python -m grb_project` | 与直接运行 `gbmtest_enhanced1_refactored.py` 等价的 CLI（参数相同）。 |

工作目录请尽量设为 **`/home/mxr/lee/gbmtest`**（或已将该目录加入 `PYTHONPATH`），以便解析 `grb_project` 及同目录下的 `modelbuild`、`separate_spectr` 等依赖。

---

## 2. `grb_project` 模块一览

| 模块 | 职责 |
|------|------|
| `session.py` | 全局路径与会话状态：`data_dir`、`catalog_xls`、`fermilat_grb_xls`、`result_root`、`summary_csv` 等；`set_result_root`；对旧脚本的 `export_legacy_aliases`。 |
| `runtime_env.py` | 3ML / numpy / warnings 等进程级初始化（`ensure_analysis_runtime`）。 |
| `logging_utils.py` | 统一 `log()`，可与会话日志文件同步。 |
| `io_utils.py` | 目录与表格：`find_files`、`ensure_dir`、`read_catalog`、`read_gcn_bn_triggers`、跳过/异常报告追加。 |
| `lat_processing.py` | LAT：读 GCN、找 FT1/FT2、gtselect / gtbin、gtrspgen、构建 `OGIPLike`。 |
| `gbm_core.py` | GBM：T90 时间窗、分 bin、本底区间、探测器选择、单探测器 TTE 插件构建。 |
| `bayesian_fit.py` | 单模型 dynesty 贝叶斯拟合与 `summary` 字典组装。 |
| `analyze_single.py` | 单暴全流程 `analyze_grb`（含 `run_overrides`）；在选定探测器与本底区间后自动尝试绘制光变图（横轴 `[t0−5, t1+5]` s，源段着色为 `[t0, t1]`，输出 `{result_root}/{grb_name}/{bnname}_lightcurve.png`）。 |
| `summary_export.py` | 汇总表后处理、最佳模型、JSON/YAML/TeX 等导出。 |
| `pipeline.py` | 批量主流程 `main`、`parse_args`、`cli_main`。 |
| `lightcurves.py` | GBM TTE + LAT FT1 联合光变：`plot_gbm_lat_lightcurve_figure`、`lightcurve_rate`、`plot_mean_lightcurve_on_ax`、道址/数据路径解析等（原 `plot_lightcurves.py` 逻辑）。 |
| `config.py` | `GRBProjectConfig`、`GRBRunOverrides` 数据类。 |
| `project.py` | `GRBProject`、`apply_project_config`；对 `pipeline.main` **延迟导入**（未装 threeML 时仍可 `import grb_project` 做配置）。 |

---

## 2.1 光变图（`lightcurves`）

- **模块**：`grb_project.lightcurves`  
- **一键出图**：`plot_gbm_lat_lightcurve_figure(bnname, ...)`  
  - 默认从 `session.data_dir/{bnname}` 匹配 `glg_tte_*` / `glg_cspec_*`；  
  - `grb_name`、`trigger_met` 可省略，此时从 `session.fermilat_grb_xls` 的 GCN 表读取；  
  - LAT 默认在 `DEFAULT_LAT_EXTENDED_ROOT`（代码内为 `/home/mxr/lee/data/fermilat/Extended_data_ex/{GRB_NAME}/`）下找 `L*EV00.fits`。  
- **命令行**：仓库根目录脚本 `plot_lightcurves.py` 已改为封装上述 API，例如：
  ```bash
  cd /home/mxr/lee/gbmtest
  python plot_lightcurves.py --bnname bn231129799 --out ./out.png
  ```
- 依赖 **threeML**（`TimeSeriesBuilder`、`step_plot` 等）；与主拟合流水线相同，不宜在仅装轻量依赖的环境中 `import grb_project.lightcurves`。

---

## 3. 路径与运行时状态

所有默认路径集中在 **`grb_project.session.AnalysisSessionState`**（单例 **`session`**）中，与原脚本顶层常量语义一致，例如：

- `data_dir`：GBM 数据根目录  
- `catalog_xls`：GBM 目录表（`fermigbrst`）  
- `fermilat_grb_xls`：含 GCN 表的 LAT/触发信息  
- `result_root`：拟合结果根目录  
- `summary_csv`：属性，由 `result_root` + `summary_csv_name` 组成  

通过 **`apply_project_config(GRBProjectConfig)`** 或 **`GRBProject.run()`** 前对 `session`/`config` 的赋值即可切换环境，无需改源码。

兼容入口 **`gbmtest_enhanced1_refactored.py`** 在 import 末尾会执行 **`export_legacy_aliases(globals())`**，使 `DATA_DIR`、`RESULT_ROOT`、`SUMMARY_CSV` 等与 `session` 同步，便于旧代码 `import gbmtest_enhanced1_refactored as m; m.DATA_DIR`。

---

## 4. 工程化配置：`GRBProject` / `GRBProjectConfig`

在代码中可先构造配置，再赋值 `bnname`、`grbname`、`t0`、`t1`、`ra`、`dec` 等，最后调用 `run()`。

```python
from grb_project import GRBProject, GRBProjectConfig

p = GRBProject()
p.config.result_root = "/path/to/results"
p.bnname = "bn221009888"
p.t0 = 0.0
p.t1 = 50.0
# grbname / ra / dec 留空则沿用 GCN / 目录表
p.run()
```

批量指定触发名：

```python
p.run(target_grbs=["bn220617772", "GRB220617A"])
```

**说明**：若在 `GRBProjectConfig` 中设置了非空的 `t0`/`t1`/`grbname` 等，`run_overrides_from_config` 会生成 **`GRBRunOverrides`**；对当前这一轮中的**每个 GRB** 若都应用相同覆盖，请确认符合你的科学意图。

---

## 5. 命令行用法

在项目根目录执行（示例路径请按本机修改）：

```bash
cd /home/mxr/lee/gbmtest

# 指定若干个 GRB（bn 或 GCN 名）
python gbmtest_enhanced1_refactored.py --grbs bn220617772 GRB220617A --analysis-mode gbm+lat

# 仅 GBM
python gbmtest_enhanced1_refactored.py --grbs bn250313607 --analysis-mode gbm

# 自定义结果根目录
python gbmtest_enhanced1_refactored.py --grbs bn221009888 --result-root /path/to/results

# GCN 表批量（与 --grbs 互斥）
python gbmtest_enhanced1_refactored.py --gcn-all --result-root /path/to/batch_results
```

等价：

```bash
python -m grb_project --grbs bn220617772 --analysis-mode gbm+lat
```

---

## 6. 依赖与环境

运行完整流水线需要（与原版脚本相同量级）：

- **Python**：建议 3.10+  
- **threeML**、`astromodels`  
- **Fermi 科学工具**：`gt_apps`、`gtrspgen` 等在系统 PATH / 环境中的配置  
- 同目录或 `PYTHONPATH` 中的 **`modelbuild`**、`separate_spectr`、`gbm_selector` 等项目内模块  

仅做 **路径配置、`import grb_project`** 时，不会因未安装 threeML 而失败（`pipeline` 在首次 `run` 时再加载）。

---

## 7. 输出摘要

在主流程结束时，一般会：

- 在 `result_root` 下按 `grb_name` 分子目录写入各模型的拟合图、FITS、`PHA`/`RSP` 相关产物；  
- 每个暴在拟合循环开始前尝试生成 **`{bnname}_lightcurve.png`**（GBM 光变 + 可选 LAT；横轴范围为 **t0−5 s 至 t1+5 s**，其中 **t0、t1** 为目录表及特例/覆盖后的光谱区间）；失败仅打日志，不中断拟合；  
- 生成/追加最佳模型与时间 bin 分析的 CSV；  
- 按 GRB 导出 `allmodel` 的 CSV/JSON/YAML 及 TeX（详见 `summary_export.py`）。

异常会追加写入结果根目录下的 **`grb_exceptions.log`**（以及 LAT 跳过时的文本报告）。

---

## 8. 与旧脚本的对应关系

| 旧习惯 | 新项目中的位置 |
|--------|----------------|
| 单文件超长脚本 | `grb_project` 多个模块，`gbmtest_enhanced1_refactored.py` 为薄封装 |
| 顶层 `DATA_DIR`、`RESULT_ROOT` | `session` + `export_legacy_aliases` |
| `main()` / `parse_args()` | `grb_project.pipeline` |
| `analyze_grb()` | `grb_project.analyze_single` |

若在二次开发时需要改物理或拟合逻辑，请优先在 **`gbm_core.py`**、**`bayesian_fit.py`**、**`lat_processing.py`** 中定位，再跑通 **`pipeline.main`**。

---

*文档与当前仓库中的 `grb_project` 布局一致；若你本地仍为单文件巨型 `gbmtest_enhanced1_refactored.py`，请先同步到拆分后的版本再对照本文。*
