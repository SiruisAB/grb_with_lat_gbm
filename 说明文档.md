# Fermi GBM + LAT 联合光谱分析 — 项目说明

本文档说明 **`grb_project`** 包的目录职责、配置方式、命令行入口，以及单次分析的**执行顺序**。物理与拟合模型集合与原版 `gbmtest_enhanced1.py` 一脉相承；实现集中在 `grb_project` 中，顶层 **`gbmtest_enhanced1_refactored.py`** 为薄封装（并导出 `DATA_DIR`、`RESULT_ROOT`、`main`、`analyze_grb` 等兼容名）。

---

## 1. 目录与入口

| 路径 | 说明 |
|------|------|
| `gbmtest/gbmtest_enhanced1_refactored.py` | **兼容入口**：CLI 与 `python -m grb_project` 一致；import 末尾 `export_legacy_aliases` 同步 `session` 到模块级常量。 |
| `grb_project/` | **核心实现包**（见第 2 节）。 |
| `python -m grb_project` | 与直接运行 `gbmtest_enhanced1_refactored.py` 等价（`__main__.py` → `pipeline.cli_main`）。 |

工作目录请设为 **`/home/mxr/lee/gbmtest`**（或将该目录加入 `PYTHONPATH`），以便解析 `grb_project` 及同级的 **`modelbuild`**、**`separate_spectr`**、**`gbm_selector`** 等依赖。

---

## 2. `grb_project` 模块一览

| 模块 | 职责 |
|------|------|
| `session.py` | 全局路径与会话状态：`data_dir`、`catalog_xls`、`fermilat_grb_xls`、`merged_xls`、`result_root`、`result_root_gcn_batch`、`summary_csv_name`、`extended_lat_data_root`、`copy_extended_lat_to_bn_dir`、`lat_extended_three_ml_pipeline` 等；`set_result_root`；`export_legacy_aliases`。 |
| `runtime_env.py` | 3ML / numpy / warnings 等进程级初始化（`ensure_analysis_runtime`）。 |
| `logging_utils.py` | 统一 `log()`，可与会话日志文件同步。 |
| `io_utils.py` | 目录与表格：`find_files`、`ensure_dir`、`read_catalog`、`read_gcn_bn_triggers`、跳过/异常报告追加；**`copy_extended_lat_to_bn_workspace`**（将 Extended LAT 复制到结果目录下 `{grb_name}/{bnname}/`）。 |
| `lat_processing.py` | LAT：读 GCN、找 FT1/FT2、gtselect / gtbin、gtrspgen、构建 `OGIPLike`（与 `session.default_lat_irfs` 可选覆盖配合，未设时默认 `p8_transient010e`）。 |
| `lat_extended_three_ml.py` | **可选** LAT Extended + **GtBurst** + **threeML** 全流程（由原 `new.py` 迁入）；`run_lat_extended_three_ml_pipeline`；耗时较长，默认关闭，见第 3、5 节。 |
| `gbm_core.py` | GBM：T90 时间窗、分 bin、本底区间、单探测器 TTE 插件构建。 |
| `gbm_detector_selection.py` | 由 trigdat / RSP 头得到卫星轴与源方向，调用 **`gbm_selector.select_best_detectors`** 得到 NaI/BGO 探测器列表。 |
| `bayesian_fit.py` | 单模型 dynesty 贝叶斯拟合与 `summary` 字典组装。 |
| `analyze_single.py` | 单暴全流程 **`analyze_grb`**（含 `run_overrides`）；步骤顺序见 **第 3 节**。 |
| `summary_export.py` | 汇总表后处理、最佳模型、JSON/YAML/TeX 等导出。 |
| `pipeline.py` | 批量主流程 **`main`**、**`parse_args`**、**`cli_main`**；读取目录表、解析 `--grbs` 与 GCN 名、循环调用 `analyze_grb`、写汇总。 |
| `lightcurves.py` | GBM TTE + LAT FT1 联合光变：`plot_gbm_lat_lightcurve_figure` 等（可由 CLI 关闭）。 |
| `config.py` | **`GRBProjectConfig`**、**`GRBRunOverrides`**、`run_overrides_from_config`。 |
| `project.py` | **`GRBProject`**、`apply_project_config`；对 `pipeline.main` **延迟导入**（未装 threeML 时仍可 `import grb_project` 做配置）。 |

---

## 3. 单次分析流程（`analyze_grb` 顺序）

下列顺序与 **`analyze_single.analyze_grb`** 一致，便于对照日志与排错。

1. **解析身份与路径**  
   从过滤后的目录表行得到 `bnname`；从 `fermilat_grb_xls` 的 `GCN` 表得 `grb_name`（可被 `run_overrides.grbname` 覆盖）。建立 **`{result_root}/{grb_name}/`** 与 **`{result_root}/{grb_name}/{bnname}/`**（后者为 `bn_workspace`）。

2. **时间与坐标**  
   `_determine_time_interval_and_position` 从目录表取 `t0, t1, ra, dec`；代码内对少数暴有硬编码特例；再由 **`GRBRunOverrides`** 覆盖 `t0/t1/ra/dec`。

3. **是否跑 LAT Extended threeML 流水线**  
   默认取 **`session.lat_extended_three_ml_pipeline`**；若 `run_overrides.lat_three_ml_full` 非空则以其为准。仅当 **`analysis_mode` 含 `lat`** 时后续 LAT 步骤有意义。

4. **复制 Extended LAT（可选）**  
   若 `analysis_mode` 含 `lat` 且 **`session.copy_extended_lat_to_bn_dir`** 为真：将 **`extended_lat_data_root/{grb_name}/`** 下数据复制到 `bn_workspace`（供后续 LAT 工具与可选 Extended 流水线使用）。

5. **LAT Extended + GtBurst + threeML（可选）**  
   若开启：在 **`chdir` 到 `{result_root}/{grb_name}`** 之后、分时间 bin 之前，调用 **`lat_extended_three_ml.run_lat_extended_three_ml_pipeline`**（FT1/FT2 解析、`LAT_dataset`、`extract_events` ROI/zmax 扫描、`TransientLATDataBuilder` 等）。失败写日志并追加 **`{result_root}/lat_three_ml_pipeline_errors.txt`**，**不中断**后续 GBM+LAT 联合拟合。

6. **时间 bin**  
   `_determine_time_bins`；个别暴在 `analyze_single` 内对 **`time_bins_list`** 有硬编码多段或多边序列。

7. **标准 LAT 插件（`lat_processing`）**  
   `process_lat_data` → `lat_plugin`。若失败则记录 **`lat_processing_skipped.txt`** 并 **return**（该暴不再拟合）。

8. **GBM 本底与探测器**  
   `_build_background_interval_string`；**`select_gbm_detectors(grb_dir)`** 得探测器列表。

9. **联合光变图（默认开启）**  
   除非 `run_overrides.plot_joint_lightcurve is False`：调用 **`lightcurves.plot_gbm_lat_lightcurve_figure`**，输出 **`{result_root}/{grb_name}/{bnname}_lightcurve.png`**（横轴约 **`[t0−5, t1+5]`** s，源段 **`[t0, t1]`**）。失败仅日志，不中断。

10. **按时间 bin × 模型循环**  
    对每个 bin：为各探测器 `_build_gbm_plugin_for_detector`，`DataList(lat_plugin, *plugins)` 或仅 GBM；模型序列为 **`band`、`blackbody`、`band+bb`、`comp`、`comp+bb`、`band+pl`、`comp+pl`**，每模型调用 **`_run_bayesian_analysis_for_model`**，结果写入 **`{result_root}/{grb_name}/{model_str}/`**。

**`pipeline.main`** 在循环结束后对汇总 DataFrame 做时间 bin 信息追加、最佳模型与 **`_save_all_models_per_grb`** 等（见 `summary_export.py`）。

---

## 4. 光变图（`lightcurves`）

- **模块**：`grb_project.lightcurves`  
- **一键出图**：`plot_gbm_lat_lightcurve_figure(bnname, ...)`  
  - 默认从 `session.data_dir/{bnname}` 匹配 `glg_tte_*` / `glg_cspec_*`；  
  - `grb_name`、`trigger_met` 可省略时从 `session.fermilat_grb_xls` 的 GCN 表读取；  
  - LAT 事件文件默认在 **`session.extended_lat_data_root/{GRB_NAME}/`** 下匹配 `L*EV00.fits` 等（与复制到结果目录的逻辑一致）。  
- **仓库根脚本**：`plot_lightcurves.py` 若仍存在，一般为对上述 API 的封装，例如：
  ```bash
  cd /home/mxr/lee/gbmtest
  python plot_lightcurves.py --bnname bn231129799 --out ./out.png
  ```
- 依赖 **threeML**；与主拟合流水线相同。

主流程中可用 **`--no-plot-joint-lightcurve`** 关闭联合光变（等价于传入 **`GRBRunOverrides(plot_joint_lightcurve=False)`**）。

---

## 5. 路径与运行时状态

默认路径在 **`grb_project.session.AnalysisSessionState`**（单例 **`session`**）中，例如：

- **`data_dir`**：GBM TTE/辅助文件根目录  
- **`catalog_xls`**：GBM 目录表（`fermigbrst`）  
- **`fermilat_grb_xls`**：含 GCN 的 LAT/触发信息  
- **`extended_lat_data_root`**：Extended LAT 原始数据根（其下为各 `{grb_name}`）  
- **`copy_extended_lat_to_bn_dir`**：是否在 LAT 分析前复制到结果子目录  
- **`lat_extended_three_ml_pipeline`**：是否默认跑完整 LAT Extended threeML 流水线（默认 `False`）  
- **`result_root`** / **`result_root_gcn_batch`**：单次与 `--gcn-all` 批量默认结果根（后者常用于 `results3`）  
- **`summary_csv`**：属性，路径为 `result_root` + **`summary_csv_name`**

通过 **`apply_project_config(GRBProjectConfig)`** 或 **`GRBProject.run()`** 前修改 `session`/`config` 即可切换环境。  
**`set_result_root(root, summary_csv_name)`** 的默认第二参数为 **`summary_results2.csv`**（与 CLI 传入自定义 `--result-root` 时 `pipeline` 的用法一致）；**`--gcn-all`** 批量则使用 **`summary_results3.csv`** 并默认开启会话日志。

兼容入口在 import 末尾执行 **`export_legacy_aliases(globals())`**，同步 `DATA_DIR`、`RESULT_ROOT`、`SUMMARY_CSV` 等。

---

## 6. 工程化配置：`GRBProject` / `GRBProjectConfig` / `GRBRunOverrides`

`GRBProjectConfig` 除路径与 **`analysis_mode`**、**`fixed_num_time_bins`** 外，还可设 **`lat_three_ml_full`**、**`plot_joint_lightcurve`**；**`apply_project_config`** 会把非空的 **`lat_three_ml_full`** 写入 **`session.lat_extended_three_ml_pipeline`**。

**`GRBRunOverrides`** 在 **`analyze_grb`** 中逐项覆盖：`grbname`、`t0/t1/ra/dec`、**`lat_three_ml_full`**（覆盖会话中的 Extended threeML 开关）、**`plot_joint_lightcurve`**（`False` 时跳过联合光变）。

```python
from grb_project import GRBProject, GRBProjectConfig

p = GRBProject()
p.config.result_root = "/path/to/results"
p.bnname = "bn221009888"
p.t0 = 0.0
p.t1 = 50.0
p.config.lat_three_ml_full = True   # 可选：跑 LAT Extended threeML
p.run()
```

批量：

```python
p.run(target_grbs=["bn220617772", "GRB220617A"])
```

若配置中对**每个**暴应用相同 `t0/t1` 等覆盖，请确认符合科学意图。

---

## 7. 命令行用法

在 **`/home/mxr/lee/gbmtest`** 下执行（路径按本机修改）：

```bash
cd /home/mxr/lee/gbmtest

# 指定若干个 GRB（bn 或 GCN 名）
python gbmtest_enhanced1_refactored.py --grbs bn220617772 GRB220617A --analysis-mode gbm+lat

# 仅 GBM
python gbmtest_enhanced1_refactored.py --grbs bn250313607 --analysis-mode gbm

# 自定义结果根目录（汇总 CSV 名默认与 set_result_root 一致，一般为 summary_results2.csv）
python gbmtest_enhanced1_refactored.py --grbs bn221009888 --result-root /path/to/results

# 将日志追加写入 {result_root}/analysis_session.log
python gbmtest_enhanced1_refactored.py --grbs bn221009888 --session-log

# 开启 LAT Extended + GtBurst + threeML 全流程（须 gbm+lat；耗时很长）
python gbmtest_enhanced1_refactored.py --grbs bn231129799 --analysis-mode gbm+lat --lat-extended-three-ml

# 关闭联合光变图
python gbmtest_enhanced1_refactored.py --grbs bn221009888 --no-plot-joint-lightcurve

# GCN 表批量（与 --grbs 互斥；未指定 --result-root 时用 session.result_root_gcn_batch；默认写 analysis_session.log 与 summary_results3.csv）
python gbmtest_enhanced1_refactored.py --gcn-all --result-root /path/to/batch_results
```

等价：

```bash
python -m grb_project --grbs bn220617772 --analysis-mode gbm+lat
```

**注意**：**`--lat-extended-three-ml`** 仅在 **`gbm+lat`** 下生效；若仅为 `gbm`，程序会打警告并跳过该流水线。

---

## 8. 依赖与环境

运行完整流水线需要：

- **Python**：建议 3.10+  
- **threeML**、`astromodels`  
- **Fermi 科学工具**：`gt_apps`、`gtrspgen` 等在 PATH 中可用  
- **GtBurst**、**fermitools**（仅在使用 **`lat_extended_three_ml`** 流水线时必需）  
- 同级或 **`PYTHONPATH`** 中的 **`modelbuild`**、**`separate_spectr`**、**`gbm_selector`**  

仅做路径配置、`import grb_project` 时，不会因未安装 threeML 而失败（**`pipeline`** 在首次 **`run`** 时再加载）。

---

## 9. 输出与日志摘要

- 在 **`result_root/{grb_name}/`** 下按模型分子目录写入拟合图、FITS、PHA/RSP 相关产物；**`{bnname}_lightcurve.png`** 默认在同目录（可被 `--no-plot-joint-lightcurve` 关闭）。  
- 汇总与最佳模型：见 **`summary_export.py`**（含按 GRB 的 `allmodel` CSV/JSON/YAML/TeX 等）。  
- **`grb_exceptions.log`**：未捕获异常。  
- **`lat_processing_skipped.txt`**：LAT 标准插件失败跳过。  
- **`lat_three_ml_pipeline_errors.txt`**：可选 Extended threeML 流水线异常。  
- **`--gcn-all`** 时可能生成 **`gcn_skipped_missing_or_invalid_trigname.txt`**、**`gcn_skipped_not_in_fermigbrst.txt`**。

---

## 10. 与旧脚本的对应关系

| 旧习惯 | 新项目中的位置 |
|--------|----------------|
| 单文件超长脚本 | `grb_project` 多模块 + `gbmtest_enhanced1_refactored.py` 薄封装 |
| 顶层 `DATA_DIR`、`RESULT_ROOT` | `session` + `export_legacy_aliases` |
| `main()` / `parse_args()` | `grb_project.pipeline` |
| `analyze_grb()` | `grb_project.analyze_single` |
| 原 `new.py` LAT Extended 流程 | `grb_project.lat_extended_three_ml` |

二次开发拟优先修改 **`gbm_core.py`**、**`bayesian_fit.py`**、**`lat_processing.py`**、**`lat_extended_three_ml.py`**（LAT 扩展分支），再跑通 **`pipeline.main`** 或 **`GRBProject.run()`**。

---

*文档与当前仓库中的 `grb_project` 代码一致；若本地入口或 `session` 默认值有改动，以 `session.py`、`pipeline.py`、`analyze_single.py` 为准。*
