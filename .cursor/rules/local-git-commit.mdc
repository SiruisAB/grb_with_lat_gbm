---
description: 修改 grb_project 后提交到本地 Git 仓库
globs: gbmtest/grb_project/**
alwaysApply: false
---

# grb_project 本地 Git 同步

仓库根目录：`/home/mxr/lee/gbmtest/grb_project`（独立 Git 仓库，分支通常为 `main`）。

完成对本目录下代码或文档的修改后，**在结束任务前**必须同步本地仓库：

1. `cd /home/mxr/lee/gbmtest/grb_project`，执行 `git status` 与 `git diff`（必要时 `git diff --staged`）。
2. 仅 `git add` 与本次任务相关的文件；不要提交密钥、`.env`、大体积结果目录或无关临时文件。
3. 使用 HEREDOC 撰写 1–2 句 commit message，说明改动目的（why），风格与 `git log` 中既有提交一致。
4. `git commit`，再 `git status` 确认本次改动已入库。
5. 除非用户明确要求，不要 `git push`。

若无有效改动可提交，在回复中简要说明即可。
